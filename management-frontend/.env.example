# Optional backend API URL if not using Vite proxy
# In Vercel set this to your deployed backend domain without /api.
# The frontend code appends /api automatically.
VITE_API_BASE_URL=https://management-dashboard-jl2k.onrender.com